name: Build signed iOS candidate

on:
  workflow_dispatch:
    inputs:
      build_number:
        description: "Unique App Store Connect build number"
        required: true
        type: string
      upload_to_testflight:
        description: "Upload to TestFlight (requires explicit owner authorization)"
        required: true
        default: "false"
        type: choice
        options:
          - "false"
          - "true"

permissions:
  contents: read

concurrency:
  group: warm-words-testflight
  cancel-in-progress: false

jobs:
  build:
    if: github.actor == github.repository_owner
    runs-on: macos-26
    environment: app-store-production
    env:
      APP_VERSION: "1.0"
      APP_BUNDLE_ID: com.dmkr.inspiraciondia.B2X6D3A9J9
      APPLE_TEAM_ID: B2X6D3A9J9

    steps:
      - name: Checkout approved branch
        uses: actions/checkout@v5

      - name: Require an approved source branch
        shell: bash
        run: |
          set -euo pipefail
          case "${GITHUB_REF_NAME}" in
            main|agent/warm-words-ios-ipa) ;;
            *)
              echo "Signed candidates may only be built from main or agent/warm-words-ios-ipa."
              exit 1
              ;;
          esac

      - name: Verify Xcode and iOS SDK
        shell: bash
        run: |
          set -euo pipefail
          xcodebuild -version
          SDK_VERSION="$(xcrun --sdk iphoneos --show-sdk-version)"
          SDK_MAJOR="${SDK_VERSION%%.*}"
          if [ "${SDK_MAJOR}" -lt 26 ]; then
            echo "iOS SDK 26 or later is required; found ${SDK_VERSION}."
            exit 1
          fi

      - name: Validate build number and required signing secrets
        shell: bash
        env:
          BUILD_NUMBER: ${{ inputs.build_number }}
          BUILD_CERTIFICATE_BASE64: ${{ secrets.BUILD_CERTIFICATE_BASE64 }}
          P12_PASSWORD: ${{ secrets.P12_PASSWORD }}
          BUILD_PROVISION_PROFILE_BASE64: ${{ secrets.BUILD_PROVISION_PROFILE_BASE64 }}
          KEYCHAIN_PASSWORD: ${{ secrets.KEYCHAIN_PASSWORD }}
        run: |
          set -euo pipefail
          if ! [[ "${BUILD_NUMBER}" =~ ^[1-9][0-9]*$ ]]; then
            echo "build_number must be a positive integer."
            exit 1
          fi
          for NAME in BUILD_CERTIFICATE_BASE64 P12_PASSWORD BUILD_PROVISION_PROFILE_BASE64 KEYCHAIN_PASSWORD; do
            if [ -z "${!NAME:-}" ]; then
              echo "Required environment secret is missing: ${NAME}."
              exit 1
            fi
          done

      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Validate content and generate the Xcode project
        shell: bash
        run: |
          set -euo pipefail
          node scripts/check-quotes.mjs
          node scripts/export-ios-content.mjs
          node scripts/export-ios-content-en.mjs
          node scripts/check-ios-closeout.mjs
          (cd native-ios && xcodegen generate)

      - name: Run unit tests without signing
        shell: bash
        run: |
          set -euo pipefail
          SIMULATOR_ID="$(xcrun simctl list devices available --json | python3 -c 'import json, sys; data = json.load(sys.stdin); print(next(device["udid"] for runtime, devices in data["devices"].items() if "iOS" in runtime for device in devices if device["isAvailable"] and device["name"].startswith("iPhone")))')"
          xcodebuild \
            -project native-ios/InspiracionDia.xcodeproj \
            -scheme InspiracionDia \
            -destination "platform=iOS Simulator,id=${SIMULATOR_ID}" \
            -derivedDataPath build/testflight-tests \
            CODE_SIGNING_ALLOWED=NO \
            test

      - name: Analyze Release configuration
        shell: bash
        run: |
          set -euo pipefail
          xcodebuild \
            -project native-ios/InspiracionDia.xcodeproj \
            -scheme InspiracionDia \
            -configuration Release \
            -sdk iphoneos \
            -derivedDataPath build/testflight-analyze \
            CODE_SIGNING_ALLOWED=NO \
            analyze

      - name: Install distribution certificate and profile
        id: signing
        shell: bash
        env:
          BUILD_CERTIFICATE_BASE64: ${{ secrets.BUILD_CERTIFICATE_BASE64 }}
          P12_PASSWORD: ${{ secrets.P12_PASSWORD }}
          BUILD_PROVISION_PROFILE_BASE64: ${{ secrets.BUILD_PROVISION_PROFILE_BASE64 }}
          KEYCHAIN_PASSWORD: ${{ secrets.KEYCHAIN_PASSWORD }}
        run: |
          set -euo pipefail
          CERTIFICATE_PATH="${RUNNER_TEMP}/distribution.p12"
          PROFILE_PATH="${RUNNER_TEMP}/distribution.mobileprovision"
          PROFILE_PLIST="${RUNNER_TEMP}/distribution-profile.plist"
          KEYCHAIN_PATH="${RUNNER_TEMP}/app-signing.keychain-db"

          printf '%s' "${BUILD_CERTIFICATE_BASE64}" | base64 --decode > "${CERTIFICATE_PATH}"
          printf '%s' "${BUILD_PROVISION_PROFILE_BASE64}" | base64 --decode > "${PROFILE_PATH}"

          security create-keychain -p "${KEYCHAIN_PASSWORD}" "${KEYCHAIN_PATH}"
          security set-keychain-settings -lut 21600 "${KEYCHAIN_PATH}"
          security unlock-keychain -p "${KEYCHAIN_PASSWORD}" "${KEYCHAIN_PATH}"
          security import "${CERTIFICATE_PATH}" -P "${P12_PASSWORD}" -A -t cert -f pkcs12 -k "${KEYCHAIN_PATH}"
          security set-key-partition-list -S apple-tool:,apple: -k "${KEYCHAIN_PASSWORD}" "${KEYCHAIN_PATH}"
          security list-keychain -d user -s "${KEYCHAIN_PATH}"

          security cms -D -i "${PROFILE_PATH}" > "${PROFILE_PLIST}"
          PROFILE_UUID="$(/usr/libexec/PlistBuddy -c 'Print :UUID' "${PROFILE_PLIST}")"
          PROFILE_NAME="$(/usr/libexec/PlistBuddy -c 'Print :Name' "${PROFILE_PLIST}")"
          PROFILE_TEAM="$(/usr/libexec/PlistBuddy -c 'Print :TeamIdentifier:0' "${PROFILE_PLIST}")"
          PROFILE_APP_ID="$(/usr/libexec/PlistBuddy -c 'Print :Entitlements:application-identifier' "${PROFILE_PLIST}")"

          if [ "${PROFILE_TEAM}" != "${APPLE_TEAM_ID}" ]; then
            echo "The provisioning profile belongs to a different Apple team."
            exit 1
          fi
          if [ "${PROFILE_APP_ID}" != "${APPLE_TEAM_ID}.${APP_BUNDLE_ID}" ]; then
            echo "The provisioning profile does not match ${APP_BUNDLE_ID}."
            exit 1
          fi

          mkdir -p "${HOME}/Library/MobileDevice/Provisioning Profiles"
          cp "${PROFILE_PATH}" "${HOME}/Library/MobileDevice/Provisioning Profiles/${PROFILE_UUID}.mobileprovision"
          echo "profile_name=${PROFILE_NAME}" >> "${GITHUB_OUTPUT}"
          echo "profile_uuid=${PROFILE_UUID}" >> "${GITHUB_OUTPUT}"
          echo "keychain_path=${KEYCHAIN_PATH}" >> "${GITHUB_OUTPUT}"

      - name: Archive signed Release build
        shell: bash
        env:
          PROFILE_NAME: ${{ steps.signing.outputs.profile_name }}
          BUILD_NUMBER: ${{ inputs.build_number }}
        run: |
          set -euo pipefail
          xcodebuild archive \
            -project native-ios/InspiracionDia.xcodeproj \
            -scheme InspiracionDia \
            -configuration Release \
            -archivePath "${RUNNER_TEMP}/WarmWords.xcarchive" \
            MARKETING_VERSION="${APP_VERSION}" \
            CURRENT_PROJECT_VERSION="${BUILD_NUMBER}" \
            DEVELOPMENT_TEAM="${APPLE_TEAM_ID}" \
            CODE_SIGN_STYLE=Manual \
            CODE_SIGN_IDENTITY="Apple Distribution" \
            PROVISIONING_PROFILE_SPECIFIER="${PROFILE_NAME}" \
            archive

      - name: Inspect archive before export
        shell: bash
        env:
          BUILD_NUMBER: ${{ inputs.build_number }}
        run: |
          set -euo pipefail
          APP_PATH="${RUNNER_TEMP}/WarmWords.xcarchive/Products/Applications/InspiracionDia.app"
          test -d "${APP_PATH}"
          test -f "${APP_PATH}/PrivacyInfo.xcprivacy"
          test -f "${APP_PATH}/Assets.car"
          test -f "${APP_PATH}/content.json"
          test -f "${APP_PATH}/content-en.json"
          /usr/bin/codesign --verify --deep --strict --verbose=2 "${APP_PATH}"
          test "$(/usr/libexec/PlistBuddy -c 'Print :CFBundleIdentifier' "${APP_PATH}/Info.plist")" = "${APP_BUNDLE_ID}"
          test "$(/usr/libexec/PlistBuddy -c 'Print :CFBundleShortVersionString' "${APP_PATH}/Info.plist")" = "${APP_VERSION}"
          test "$(/usr/libexec/PlistBuddy -c 'Print :CFBundleVersion' "${APP_PATH}/Info.plist")" = "${BUILD_NUMBER}"
          test "$(/usr/libexec/PlistBuddy -c 'Print :CFBundleDisplayName' "${APP_PATH}/Info.plist")" = "Warm Words"

      - name: Create App Store Connect export options
        shell: bash
        env:
          PROFILE_NAME: ${{ steps.signing.outputs.profile_name }}
        run: |
          set -euo pipefail
          plutil -create xml1 "${RUNNER_TEMP}/ExportOptions.plist"
          /usr/libexec/PlistBuddy -c 'Add :method string app-store-connect' "${RUNNER_TEMP}/ExportOptions.plist"
          /usr/libexec/PlistBuddy -c 'Add :signingStyle string manual' "${RUNNER_TEMP}/ExportOptions.plist"
          /usr/libexec/PlistBuddy -c "Add :teamID string ${APPLE_TEAM_ID}" "${RUNNER_TEMP}/ExportOptions.plist"
          /usr/libexec/PlistBuddy -c 'Add :stripSwiftSymbols bool true' "${RUNNER_TEMP}/ExportOptions.plist"
          /usr/libexec/PlistBuddy -c 'Add :uploadSymbols bool true' "${RUNNER_TEMP}/ExportOptions.plist"
          /usr/libexec/PlistBuddy -c 'Add :manageAppVersionAndBuildNumber bool false' "${RUNNER_TEMP}/ExportOptions.plist"
          /usr/libexec/PlistBuddy -c 'Add :provisioningProfiles dict' "${RUNNER_TEMP}/ExportOptions.plist"
          /usr/libexec/PlistBuddy -c "Add :provisioningProfiles:${APP_BUNDLE_ID} string ${PROFILE_NAME}" "${RUNNER_TEMP}/ExportOptions.plist"

      - name: Export signed IPA
        shell: bash
        run: |
          set -euo pipefail
          xcodebuild -exportArchive \
            -archivePath "${RUNNER_TEMP}/WarmWords.xcarchive" \
            -exportOptionsPlist "${RUNNER_TEMP}/ExportOptions.plist" \
            -exportPath "${RUNNER_TEMP}/WarmWords-export"
          test -f "${RUNNER_TEMP}/WarmWords-export/InspiracionDia.ipa"

      - name: Store signed candidate artifact
        uses: actions/upload-artifact@v6
        with:
          name: Warm-Words-TestFlight-v${{ env.APP_VERSION }}-build-${{ inputs.build_number }}
          path: ${{ runner.temp }}/WarmWords-export/InspiracionDia.ipa
          retention-days: 3

      - name: Require upload credentials
        if: inputs.upload_to_testflight == 'true'
        shell: bash
        env:
          APP_STORE_CONNECT_API_KEY_ID: ${{ secrets.APP_STORE_CONNECT_API_KEY_ID }}
          APP_STORE_CONNECT_ISSUER_ID: ${{ secrets.APP_STORE_CONNECT_ISSUER_ID }}
          APP_STORE_CONNECT_API_KEY_BASE64: ${{ secrets.APP_STORE_CONNECT_API_KEY_BASE64 }}
        run: |
          set -euo pipefail
          for NAME in APP_STORE_CONNECT_API_KEY_ID APP_STORE_CONNECT_ISSUER_ID APP_STORE_CONNECT_API_KEY_BASE64; do
            if [ -z "${!NAME:-}" ]; then
              echo "Required upload secret is missing: ${NAME}."
              exit 1
            fi
          done

      - name: Upload explicitly authorized build to TestFlight
        if: inputs.upload_to_testflight == 'true'
        shell: bash
        env:
          APP_STORE_CONNECT_API_KEY_ID: ${{ secrets.APP_STORE_CONNECT_API_KEY_ID }}
          APP_STORE_CONNECT_ISSUER_ID: ${{ secrets.APP_STORE_CONNECT_ISSUER_ID }}
          APP_STORE_CONNECT_API_KEY_BASE64: ${{ secrets.APP_STORE_CONNECT_API_KEY_BASE64 }}
        run: |
          set -euo pipefail
          PRIVATE_KEYS_DIR="${HOME}/.appstoreconnect/private_keys"
          mkdir -p "${PRIVATE_KEYS_DIR}"
          API_KEY_PATH="${PRIVATE_KEYS_DIR}/AuthKey_${APP_STORE_CONNECT_API_KEY_ID}.p8"
          cleanup_api_key() {
            rm -f "${API_KEY_PATH}"
            rmdir "${PRIVATE_KEYS_DIR}" 2>/dev/null || true
          }
          trap cleanup_api_key EXIT
          printf '%s' "${APP_STORE_CONNECT_API_KEY_BASE64}" | base64 --decode > "${API_KEY_PATH}"
          chmod 600 "${API_KEY_PATH}"
          xcrun altool --validate-app \
            --type ios \
            --file "${RUNNER_TEMP}/WarmWords-export/InspiracionDia.ipa" \
            --apiKey "${APP_STORE_CONNECT_API_KEY_ID}" \
            --apiIssuer "${APP_STORE_CONNECT_ISSUER_ID}"
          xcrun altool --upload-app \
            --type ios \
            --file "${RUNNER_TEMP}/WarmWords-export/InspiracionDia.ipa" \
            --apiKey "${APP_STORE_CONNECT_API_KEY_ID}" \
            --apiIssuer "${APP_STORE_CONNECT_ISSUER_ID}"

      - name: Remove temporary signing material
        if: always()
        shell: bash
        env:
          KEYCHAIN_PATH: ${{ steps.signing.outputs.keychain_path }}
          PROFILE_UUID: ${{ steps.signing.outputs.profile_uuid }}
        run: |
          if [ -n "${KEYCHAIN_PATH:-}" ]; then
            security delete-keychain "${KEYCHAIN_PATH}" || true
          fi
          if [ -n "${PROFILE_UUID:-}" ]; then
            rm -f "${HOME}/Library/MobileDevice/Provisioning Profiles/${PROFILE_UUID}.mobileprovision"
          fi
